// Local development config
// Copy this file to config.js and set your tunnel URL
// This file is git-ignored - config.js.example is committed as a template
//
// Usage:
//   cp config.js.example config.js
//   # Edit config.js with your tunnel URL
//   npm run dev

window.AUDIOBOOKSHELF_CONFIG = {
  // Set this to your Cloudflare tunnel URL to test against remote backend
  // Leave empty or remove to use local Vite proxy (http://localhost:8081)
  tunnelUrl: '',

  // Example with tunnel:
  // tunnelUrl: 'https://your-tunnel-name.trycloudflare.com',

  // Set this to your LAN backend URL for LAN-first connection
  // The app will try this URL first to save bandwidth, falling back to tunnelUrl
  // Example: 'http://192.168.1.100:8081'
  localUrl: '',

  lastUpdated: 'local-dev'
};
